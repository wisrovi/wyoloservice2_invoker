version: '3.8'

services:
  test_sender:
    build: .
    container_name: test_sender
    environment:
      - REDIS_URL=redis://192.168.1.137:23437/0
      - QUEUE_NAME=gpus_high
    network_mode: host
